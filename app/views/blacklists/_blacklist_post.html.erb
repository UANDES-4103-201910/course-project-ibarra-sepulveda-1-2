<% @blacklists.each do |blacklist| %>
	<div class="col-sm-6">
		<div class="col-12"><br></div>
		<div class="container border border-dark">
			<div class="col-12"><br></div>
			<div class="col-12">
				<div class="row">
					<div class="col-11"></div>
					<div class="col-1">
						<%= link_to fa_icon("times 2x"), blacklist, method: :delete, data: { confirm: 'Are you sure?'} %>
					</div>
				</div>
			</div>
			<div class="row">
			    <div class="col-2"> 
			    	<figure class="figure">
			    		 <%= image_tag User.where(id: blacklist.user_id )[0].picture.medium.url, class: 'figure-img img-fluid rounded'%>
			    	</figure>   
			    </div>
			    <div class="col-10">
			      <div class="col-12 card-title">
			        <h4><%= User.where(id: blacklist.user_id )[0].email %></h4>
			      </div>
			      <div class="col-12">
			        <%= blacklist.updated_at %>
			      </div>
			      <div class="col-12">
			         <i class="fa fa-map-marker"></i> <%= User.where(id: blacklist.user_id )[0].city %>, <%= User.where(id: blacklist.user_id )[0].country %> 
			      </div>
			    </div>
			</div>
			<div class="col-12"><br></div>
	  	</div>
	  	<div class="col-12"><br></div>
	</div>

<% end %>
